import { ChakraProvider as BaseChakraProvider } from '@chakra-ui/provider';
import { theme } from '@fidesui/react-theme';

export const FidesProvider = BaseChakraProvider;
FidesProvider.defaultProps = {
  theme,
};
export type { ChakraProviderProps as FidesProviderProps } from '@chakra-ui/provider';
